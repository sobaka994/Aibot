[package]
name = "core_os"
version = "0.1.0"
edition = "2021"

[dependencies]
uefi = { version = "=0.25.0", features = ["alloc"] }
uefi-services = "=0.22.0"
log = "0.4.20"
noto-sans-mono-bitmap = { version = "0.3", features = ["all"] }
